# PE Falcon Safaris — public client (Next.js)
# Netlify reads this file from the repository root on deploy.

[build]
  base = "client"
  command = "npm install && npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
