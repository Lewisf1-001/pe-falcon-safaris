# PE Falcon Safaris — admin panel (Next.js)
# Netlify reads this file from the repository root on deploy.

[build]
  base = "admin"
  command = "npm install && npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
